# Route ALSA (copperline's cpal audio backend) to the container's pulseaudio
# daemon, which KasmVNC picks up and streams to the browser. The pulse plugin
# finds the daemon via PULSE_SERVER (unix:/tmp/pulse-socket, set in the
# Dockerfile).
pcm.!default {
    type pulse
}
ctl.!default {
    type pulse
}
